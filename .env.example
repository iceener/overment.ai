# Public frontend config. Values here are baked into dist/ and visible in the browser.
# Do not put secrets or provider API keys here.
PUBLIC_NEWSLETTER_ENDPOINT=https://alice.overment.com/api/newsletter/subscribe
